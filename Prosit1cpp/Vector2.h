#pragma once

#include <iostream>
#include <math.h>
float oa, ob;

class Vector2 {
    
    private:
/*A COMPLETER*/
  
    public:
/*A COMPLETER*/

    Vector2 (int x, int y): x(x), y(y) {}//Tentative de constructeur
    //Vector2(int x, int y): x(x), y(y) {}
    Vector2(int x, int y);
    //set_Vector2(int, int) ;
    //Vector2_cross();
};
