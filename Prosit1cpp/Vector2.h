#pragma once

#include <iostream>
#include <math.h>

class Vector2 {
    

  
    public:
/*A COMPLETER*/

    int x;
    int y;
    
    Vector2 (int x, int y): x(x), y(y) {}//Tentative de constructeur
    //Vector2(int x, int y): x(x), y(y) {}
    //Vect2(int x, int y);
    set_Vector2(int, int) ;
    //Vector2_cross();
};
